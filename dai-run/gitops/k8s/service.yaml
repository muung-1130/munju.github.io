apiVersion: v1
kind: Service
metadata:
  name: dairun-application
  namespace: dairun
spec:
  type: ClusterIP
  selector:
    app: dairun-application
  ports:
    - name: http
      port: 80
      targetPort: http