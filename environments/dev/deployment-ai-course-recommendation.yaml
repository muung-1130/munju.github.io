apiVersion: apps/v1
kind: Deployment
metadata:
  name: dai-run-ai-course-recommendation
  labels:
    app.kubernetes.io/name: dai-run-ai-course-recommendation
    app.kubernetes.io/part-of: dai-run
    app.kubernetes.io/component: ai-course-recommendation
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: dai-run-ai-course-recommendation
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dai-run-ai-course-recommendation
        app.kubernetes.io/part-of: dai-run
        app.kubernetes.io/component: ai-course-recommendation
        dairun.io/track: blue
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: harbor-dai-run-pull
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ai-course-recommendation
          image: harbor.dai-run.internal/dai-run/ai-course-recommendation:pending-first-build
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: dai-run-ai-course-recommendation-config
            - secretRef:
                name: dai-run-ai-course-recommendation-db
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          resources:
            requests:
              cpu: 300m
              memory: 512Mi
            limits:
              cpu: 1500m
              memory: 1536Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            failureThreshold: 24
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
