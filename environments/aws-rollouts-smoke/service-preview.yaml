apiVersion: v1
kind: Service
metadata:
  name: dairun-application-rollout-preview
  labels:
    app.kubernetes.io/name: dairun-application-rollout-smoke
    app.kubernetes.io/part-of: dai-run
    dairun.io/role: preview
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dairun-application-rollout-smoke
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
